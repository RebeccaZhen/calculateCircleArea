#define _USE_MATH_DEFINES

#include <cmath>
#include <iostream>

int main() {
	std::cout << "Enter radius in metres: "
	int radius
	std::cin >> radius;
	std::cout << M_PI * radius * radius;
	return 0;
}